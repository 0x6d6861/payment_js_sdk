/**
 * All supported currencies
 * @enum {string}
 */
export enum CURRENCIES {
  KES = 'KES',
  UGX = 'UGX',
}

/**
 * All supported payment providers
 * @enum {string}
 */
export enum PROVIDERS {
  MPESA = 'MPESA',
  CARDS = 'CARDS',
}
